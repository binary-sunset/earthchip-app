<%= simple_form_for device do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>

  <%= f.button :submit, class: "btn-primary" %>
  <%= link_to 'Cancel', device, class: "btn btn-secondary" %>
<% end %>